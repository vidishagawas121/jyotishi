{
  "name": "sangam-jyotish-sansthan",
  "version": "1.0.0",
  "private": true,
  "description": "Sangam Jyotish Sansthan - Fullstack Vedic Astrology Platform (Client + Server)",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "dev:all": "concurrently -n \"CLIENT,SERVER\" -c \"cyan,magenta\" \"npm --prefix client run dev\" \"npm --prefix server run dev\"",
    "build": "npm --prefix client run build",
    "preview": "npm --prefix client run preview",
    "typecheck": "npm --prefix client run typecheck",
    "server": "npm --prefix server start",
    "test:db": "npm --prefix server run test:db"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
